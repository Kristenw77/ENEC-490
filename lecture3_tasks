
function [m] = monthly_stats(d)

matrix = zeros(12,2)
    for j = 1:12
        matrix(j,1) = mean(d(j,:))
        matrix(j,2) = std(d(j,:))
    end
    
  % looks good, but wanted to see it with the Lecture 3 m-file (plotting histograms)

